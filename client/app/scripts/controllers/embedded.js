GLClient.controller('EmbeddedReceiptCtrl',
  ['$rootScope',
  function($rootScope) {

  $rootScope.embedded = true;

}]).
controller('EmbeddedSubmissionCtrl',
  ['$rootScope',
  function($rootScope) {

  $rootScope.embedded = true;

}]);

